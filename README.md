# Aura Frozen Coconut — Next.js Starter

This repository contains a Next.js + TypeScript + TailwindCSS starter focused on a premium UI for Aura Frozen Coconut.

Run locally:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
npm start
```

Deploy on Vercel for best results.
